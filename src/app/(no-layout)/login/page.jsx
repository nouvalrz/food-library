import React from "react";
import LoginClient from "./LoginClient";

export const metadata = {
  title: "Login",
};

const page = () => {
  return (
    <div>
      <LoginClient />
    </div>
  );
};

export default page;
